Install ncurses first, otherwise ncurses link during compilation will fail

```bash
sudo apt update
sudo apt install libncurses-dev pkg-config build-essential
cd /mnt/c/Users/mowan/source/repos/world_knowledge_structure/BookkeepingRust
cargo run
```