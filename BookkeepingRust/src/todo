1. Check correctness and use Result<T, E> instead of assert_eq! (panic), change HashSet to Vec if its properties not used, etc.

1. Implement a lazy-load iterator
1.1 Implement a lazy-load iterator in tree-array. Consecutive queries produces O(m*log(n)), while an iterator provides O(log(n) + m). n is the size of tree-array and m is the size of number of queried elements
1.2 Implement a lazy-load iterator fetch for both ordered_table and unordered_table

2. Add more utility methods for ordered_table and unordered_table