//! Test target for the `domain` layer; mirrors `src/domain/`.

mod domain {
    mod cell_segments;
    mod ordered_table;
    mod table_column;
    mod table_row_iter;
    mod unordered_table;
}
