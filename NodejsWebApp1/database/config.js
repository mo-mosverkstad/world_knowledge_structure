const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "localhost",
        user: "root",
        password: "mysql",
        database: "exampleDB",
    }
};
module.exports = config;